# 重構規劃與本輪實作

日期：2026-09-12。起始提交：`ff4a2e3`。

## 目標與相容性

將「資料與規則」「功能決策」「遊戲輸入與檔案傳輸」「操作介面」逐步分開，讓修改拾取或跟隨時，不必同時修改整個主迴圈。

本輪完成共用模組拆分、拾取決策抽取與請求生命週期封裝。CLI、JSON 設定格式、IPC schema、探針 DLL 與現有遊戲操作順序維持相容；舊入口繼續匯出既有資料模型與功能函式。測試與新程式可直接匯入所屬模組。

本輪不重寫 C# 遊戲輸入、RPC 或安裝流程，也不將未完成的功能放入空殼 controller。UI 與其餘模式的主迴圈拆分列於後續階段。

## 階段一：共用邊界與拾取決策（本輪已完成）

| 模組 | 職責 | 不承擔的工作 |
| --- | --- | --- |
| `spiritvale_models.py` | 世界快照、目標與各功能狀態資料 | 檔案、視窗、遊戲輸入 |
| `spiritvale_config.py` | 設定 schema、預設值、驗證、設定 JSON 讀寫 | 遊戲流程決策 |
| `spiritvale_snapshot.py` | 快照 schema／時效／欄位解析與診斷資訊 | 讀取狀態檔、送出輸入 |
| `spiritvale_ipc.py` | Windows 共享檔案讀取、原子寫入、等待快照、請求發布 | 選怪、拾取優先順序、畫面 |
| `spiritvale_navigation.py` | 選怪、跟隨距離、路徑幾何、出口避讓與戰鬥恢復計算 | 實際送鍵與 IPC 寫入 |
| `spiritvale_loot.py` | 掉落物篩選、距離規則、接近／脫困與拾取準備決策 | 執行按鍵、寫 IPC、暫停刷王或顯示視窗 |
| `spiritvale_upkeep.py` | 召喚／Buff／坐騎状态與職業技能時序；沿用可注入的技能觸發 callback | 固定綁定 Windows 或探針傳輸 |
| `spiritvale_earnings.py` | 收入與運行時間計算 | 查詢錢包或顯示視窗 |
| `spiritvale_red_dot_bot.py` | 啟動入口、模式協調、安全停止、輸入執行、現有 UI | 已抽離的規則實作 |

核心依賴方向：入口 → 決策／IPC → 模型與設定。新模組均不反向匯入入口。

### 拾取流程

`plan_loot_interaction` 回傳確認、接近、放鍵等待、互動或停止決策，並更新傳入的拾取準備狀態。它不呼叫視窗 API，也不寫入 IPC。只有正式執行且放鍵等待與冷卻條件符合，才回傳可執行的物件 ID；預覽只回報應有行為。

`plan_loot_approach` 驗證對應請求的路徑、檢查出口安全圈、更新脫困狀態，回傳移動鍵、路徑是否就緒、失敗原因及通知。主迴圈依一般／刷王模式決定失敗後是延後重試還是安全暫停。

主迴圈仍負責：選定目標、出現／消失確認、打斷戰鬥時凍結其計時、安全條件與模式切換時重設狀態，以及執行決策。這些剩餘責任將在第二階段一起處理，避免只拆出類別名稱卻繼續共享整包外部狀態。

### IPC 請求流程

`NavigationRequestPublisher` 集中保存請求編號、前次目標與輸入簽章、最後成功寫入時間，取代主流程十多個 `last_request_*` 變數。

- 相同意圖在 500 ms 內不重複寫入，超過間隔則補心跳。
- WASD／Shift 變化、拾取重試不改變尋路 request ID。
- 目標、模式、技能、換頻或消耗品請求等條件變更時，產生新的 request ID。
- 強制寫入可用於停止；寫入失敗時不提前消耗編號或抑制重試。
- 寫入器可注入；等待隊伍／錢包快照也可注入讀取器。

入口中的 `request_target` 改名為 `publish_navigation_intent`，負責把目前模式、輸入與一次性動作組成完整請求，傳輸與去重交給 publisher。

### 測試與交付

- 保留原有 218 項 Python 測試，僅調整移動後的 mock 所屬模組與標準函式引用。
- 新增 14 項拾取決策測試：確認、放鍵、邊界抖動、離開後重進、預覽、跨目標冷卻、暫停／死亡、出口、舊路徑、錯誤目標類型與脫困。
- 新增 6 項 publisher 測試：心跳、去重、放鍵、拾取、模式／目標／技能改變、強制停止及寫入失敗重試。
- 新增 3 項模組／發佈邊界測試：純決策模組不需要 site-packages、舊匯入名稱保持同一物件、發佈清單包含所有間接依賴並能在隔離目錄執行 `--help`。
- Python 合計 **241 項通過**；現有 DLL 的 GuardianBond **13 項**與拾取 **21 項**行為檢查通過。
- 主檔由 6,734 行降至約 4,038 行；`run_bot` 仍約 2,617 行，後續控制器拆分仍有必要。
- `build_release.ps1` 已加入全部新模組。正式 DLL、個人設定均未更動；本輪沒有啟動實際遊戲操作或重啟既有 BOT。
- 已建置 `dist/SpiritValeBot-v2.23.4-refactor-win64.zip`，確認 ZIP 完整性、主程式／8 個模組與工作區內容一致，且裝備篩選設定使用安全預設值。
- 對照起始提交的 100 個搬移定義，除兩個快照等待函式新增可注入讀取器外，其餘函式／模型的 AST 相同。新增決策與 publisher 另由行為測試覆蓋。

## 階段二：集中模式協調與輸入執行（後續）

先將選單、雷達與價格顯示移至 UI 模組，以命令與顯示資料連接控制器。接著依序建立拾取、跟隨、一般戰鬥、刷王與職業維護控制器，每個控制器持有自己的狀態，提供 `step(snapshot, now)` 與明確的進入／中斷／退出處理。

中央協調器保留現有優先順序：安全條件 → 職業維護 → 純跟隨或一般模式；一般模式內再協調刷王、拾取與戰鬥。所有動作由單一執行端送出，避免控制器直接互相清鍵或修改其他控制器的計時。

每拆一個控制器就補一組跨模式行為測試，特別檢查：拾取途中死亡／斷線、刷王中切換跟隨、補 Buff 時暫停、舊請求回覆到達、關閉程式時停止輸入。確認前後輸入序列與狀態轉移相同後，再刪除舊分支。

完成標準：主迴圈只負責讀取、決策協調、執行與呈現，功能狀態不再由多個分支直接修改。

## 階段三：C# 探針拆分（後續）

先依現有服務類別拆成多個 `.cs` 檔，保持 namespace、類別名稱與反射入口不變，並更新建置腳本的明確來源清單。之後將 `PlayerUpdatePatch` 中的世界掃描、掉落物快取、NavMesh 與快照輸出分離。

遊戲輸入、技能與拾取 RPC 最後處理。每次變更都重新建置 DLL，跑 Python 協定測試和 C# 行為測試，再安排實機驗證；單純拆檔不宣稱已完成職責重構。
