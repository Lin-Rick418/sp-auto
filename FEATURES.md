# SpiritVale 功能整理

整理日期：2026-09-12。依程式碼與目前設定檔整理，功能基準提交為 `eaf1f9a`。

## 系統架構

這是 Windows 上的 SpiritVale 遊戲自動化工具。Python 負責控制選單、導航決策與背景工作，BepInEx IL2CPP 探針負責讀取遊戲物件、計算 Unity NavMesh 路徑，以及呼叫遊戲既有的輸入與物品操作流程。兩端以 JSON 檔案交換請求與狀態。

目前導航依據記憶體中的世界座標，不使用遊戲截圖或小地圖顏色辨識。OpenCV 與 Pillow 主要用於顯示雷達、價格等介面。

## 已實作功能

| 功能 | 實際行為與邊界 |
| --- | --- |
| F8 控制選單 | 遊戲視窗在前景時按 F8 開啟選單。可切換導航、跟隨、拾取，設定拾取品質與召喚檢查，開啟裝備篩選、Card 購買及價格視窗，重新查價或安全結束。 |
| 正常打怪模式 | 尋找最近的有效敵怪，以 ObjectId 持續鎖定，沿 NavMesh 路徑接近；抵達攻擊距離後等待怪物死亡或失效，再選下一隻。 |
| 火車模式 | 抵達怪物後立即切換下一隻，並暫時略過原目標，適合連續移動引怪。 |
| 自動刷王模式 | 依設定使用召喚消耗品、等待指定名稱的王、戰鬥、確認王消失、等待掉落，再拾取自己的 Legendary 非裝備物品，完成後重複。召喚失敗、逾時等錯誤會暫停。 |
| 背景移動與技能 | 依相機方向換算 WASD；探針同步世界移動與 Shift 技能輸入，支援遊戲失焦或最小化時繼續更新。可選擇鎖定怪物，以及每次取得新目標後背景左鍵點擊一次。 |
| 職業輸入模式 | `job_type=0`：一般導航長按 Left Shift；`1`：長按左右 Shift；`2`：成功鎖怪後，依隨機間隔短按 Left Shift。技能仍依遊戲熱鍵綁定執行。 |
| 召喚物／Buff 維護 | 召喚職業可逐項設定召喚物與 Buff 的 NumPad 技能鍵；缺少時先停步、逐項補放並等待確認。GuardianBond 會確認自己的存活召喚物及對外連結。另有先按 9、再按 0 的坐騎維護與重試流程。 |
| 純隊友跟隨 | 以完整角色名稱選擇隊伍成員，再以 PlayerId 鎖定，保持碰撞半徑加緩衝距離。可在同地圖不同實例／頻道間切換以追上隊友。此模式不選怪或撿物，路徑有效時維持左右 Shift。 |
| 自動拾取 | 可設定 Common、Rare、Unique、Legendary 以上。合格的自有遠距物品會主動導航接近；非自有物品只在自然進入有效距離且未鎖定時處理。所有地面裝備均排除。 |
| 指定物品互動 | 先停步與放開技能，再透過 `InteractableId` 指向單件掉落物送出點擊；依快照確認物品是否仍存在並冷卻重試。含出現／消失確認、追蹤逾時與距離邊界遲滯處理。 |
| 首領迴避 | 將「存活敵怪中等級最高且只有一隻」視為首領，支援換頻或遠離。頻道資料不可用時可退回遠離行為；自動刷王使用指定名稱目標的獨立流程。 |
| 防卡住與戰鬥停滯處理 | 以世界座標偵測位移停滯，嘗試倒退、側移等脫困；追蹤逾時或脫困失敗時換目標。戰鬥中監測掉血，無進展時脫離重接，持續失敗則封鎖該目標。 |
| 傳送出口避讓 | 一般怪物與掉落物導航排除出口安全圈中的目標，並拒絕穿越安全圈的路徑；已在圈內時只允許往外離開。純跟隨不套用此限制。 |
| 背包裝備自動篩選 | 每個基礎物品 ID 各自保存詞條最低值與「至少符合 N 項」規則，提供中文詞條搜尋及編輯。符合者收藏，不符合者分解；可設定某名稱一律分解。 |
| 裝備保護 | 已收藏裝備直接略過，未設定有效規則的名稱略過。收藏／分解以 UID、物品 ID、位置及請求 ID 核對；分解端再次拒絕收藏品及穿戴中的物品。可定期執行或手動立即執行。 |
| 拍賣查詢 | 不需開啟一般拍賣 UI。命令列可按名稱、價格、精煉、等級、潛力、類型、能力等條件搜尋，支援分頁與 JSON 輸出；能力條件為最低值。 |
| 背包精確查價 | 讀取最新未收藏背包装備，以基礎物品 ID／名稱及遊戲轉換後完整顯示詞條比對拍賣。提供最低價、中位價、樣本數與無相同樣本標示；相同裝備合併顯示，高價優先。 |
| Card 持續限價購買 | 輸入保留金額與單價上限，只購買單價嚴格低於上限的 Card；依可支用餘額決定數量。成交後繼續搜尋，沒有合格項目時等待重查，顯示數量、花費與餘額。 |
| 購買停止與結果處理 | 單筆購買檢查預期價格與上架版本；結果不明或逾時時停止且不重送。按停止後不建立新請求，但已送出的交易仍可能完成。 |
| 斷線重登 | 正式導航或跟隨啟用時，先等待遊戲內建重連，再透過登入／選角流程選回原角色 UID。支援嘗試逾時、重試間隔及次數上限。 |
| 安全停止 | 死亡、快照過期、資料錯誤、地圖切換或無有效路徑時停止相關導航輸入；退出時釋放按鍵。Python 心跳失效後探針停止自動化輸入與世界掃描。 |
| 雷達、位置與收入資訊 | 可選擇顯示記憶體雷達；獨立工具輸出位置與怪物等資訊。每次一般導航統計運行時間、金幣餘額上升總額與目前餘額，排除純跟隨期間。 |
| 安裝與發佈 | PowerShell 建置 Loader／探針，提供一鍵安裝、正式／預覽啟動腳本與 ZIP 打包。發佈包使用預設裝備篩選檔，不攜帶本機個人篩選規則。 |

## 目前 repo 的設定

以下是提交中的實際值，與程式內建預設值不一定相同。

| 設定 | 目前值 |
| --- | --- |
| 導航模式 | `mode=2`，火車模式 |
| 啟動狀態 | `start_paused=true`，一般導航先待機 |
| 職業 | `job_type=1`，召喚 |
| 怪物鎖定 | 開啟背景鎖怪；取得目標後的額外左鍵點擊關閉 |
| 拾取品質 | Legendary 以上 |
| 拾取距離調整 | `-0.2` 世界單位；遲滯 `0.25`；放鍵等待 `50 ms`；互動重試間隔 `500 ms` |
| 首領迴避 | 開啟，使用 `switch_channel` |
| 斷線重登 | 開啟，最多 5 次 |
| 查價 | 功能開啟，啟動時不自動查價 |
| 雷達 | 關閉，可透過 `debug_window` 啟用 |
| 召喚檢查 | Skeleton、Abomination、Skeleton Mage、Conjurer、DeathBramble、GuardianBond 開啟；Invoker 關閉 |
| 裝備篩選 | 本機規則已啟用，週期 100 秒，含個別名稱一律分解設定 |
| 刷王參數 | 保存 Lady Fey／Sunborn Petal Tuft；目前為模式 2，因此不啟動刷王流程 |

## 主要程式對照

| 檔案 | 責任 |
| --- | --- |
| `spiritvale_red_dot_bot.py` | 主程式、控制選單、導航／戰鬥／跟隨／刷王／拾取狀態流程、職業維護、雷達與收入統計 |
| `SpiritValePositionProbe.cs` | 遊戲內資料讀取、NavMesh、背景輸入、物品互動、拍賣／背包 IPC、換頻與重登 |
| `SpiritValeProbeLoader.cs` | 隨遊戲啟動的輕量 Loader，收到新鮮請求後才載入完整探針 |
| `spiritvale_paths.py` | LocalAppData IPC 路徑、舊路徑相容與探針載入握手 |
| `spiritvale_equipment_filter.py` | 裝備規則編輯器、規則判斷、收藏／分解排程 |
| `spiritvale_inventory_pricer.py` | 背包讀取、拍賣比對與價格報告 |
| `spiritvale_auction_query.py` | 拍賣搜尋命令列工具與共享查詢鎖 |
| `spiritvale_card_buyer.py` | Card 大量購買控制器與視窗 |
| `spiritvale_equipment_favorite.py` | 獨立的穿戴裝備收藏狀態操作工具 |
| `spiritvale_inventory_dismantle.py` | 獨立的精確指定背包装備分解工具 |
| `spiritvale_position_reporter.py` | 獨立位置／地圖／怪物資訊輸出 |
| `build_position_probe.ps1`、`install_spiritvale_bot.ps1` | 探針建置與安裝 |
| `build_release.ps1`、`run_spiritvale_bot.ps1`、`*.cmd` | 打包與啟動入口 |
| `test_*.py`、`diagnostics/` | 單元測試、C# 行為測試與既有調查資料 |

## 功能邊界與閱讀注意事項

- 實際快捷鍵入口只有 F8 控制選單。README 後段的 F2／F4／F5／F6／F7／F9／Del 是歷史名稱，不應當作仍然有效的獨立快捷鍵。
- 最新拾取使用指定物件的遊戲互動輸入，並非 README 開頭提到的短按 V。送出互動請求不代表伺服器已接受拾取。
- 已移除影像校正、光柱辨識、藍色動態危險區判斷，以及地面裝備拾取前估價；背包装備查價仍存在。
- 跟隨僅能選隊友。同地圖可追隨實例／頻道變更；隊友到不同地圖時，目前流程顯示狀態並等待，沒有跨地圖尋路功能。
- 刷王的自有 Legendary 拾取是循環中的必要階段，不受一般拾取開關或 `--no-loot` 停用。
- 首領判斷採最高等級唯一性的推測，並非遊戲權威的首領旗標。
- 收入統計只累加金幣餘額增加，支出不扣除，因此不是淨利；目前不保存歷史報表。
- 重登不會重啟已關閉或崩潰的遊戲。
- IPC 預設共用 `%LOCALAPPDATA%\SpiritValeBot`。F8 有前景視窗限制，但這本身不代表多開程序已具備完整的資料隔離。
- 多項功能依賴遊戲內部型別與方法，遊戲更新後仍需重新確認探針相容性。

## 本次驗證

執行 `.venv\Scripts\python.exe -m unittest discover -v`：**218 項測試全部通過**。

本次為程式碼閱讀、設定核對與單元測試；未啟動實際遊戲操作，也未重新建置 DLL 或重跑 C#／實機診斷。既有診斷結果不等同於本次重新驗證。
